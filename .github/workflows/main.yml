name: Disable Worlflow Video

on:
  push:
     branches: [ main ]

jobs:
  disable:
     runs-on: ubuntu-latest

     steps:
        - name: Sakshi
          run: echo "Disable Workflow Video DevOps"
